import { Bar, BarChart, CartesianGrid, Cell, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts'
import type { SimulationResult } from '../../entities/simulation/types'

export function ProbabilityChart({result}:{result:SimulationResult}){const p=result.winDrawLoseProbability,data=[{name:'승리',value:Math.round(p.win*100),color:'#4ade80'},{name:'무승부',value:Math.round(p.draw*100),color:'#a1a1aa'},{name:'패배',value:Math.round(p.loss*100),color:'#f87171'}];return <section className="rounded-2xl border border-white/10 bg-zinc-900/70 p-5 sm:p-7"><div className="flex flex-wrap items-end justify-between gap-3"><div><p className="text-xs font-black uppercase tracking-[.18em] text-green-400">Probability model</p><h2 className="mt-1 text-xl font-black">전술 선택에 따른 결과 가능성</h2></div><div className="text-right"><b className="text-3xl text-green-400">{Math.round(result.successProbability*100)}%</b><p className="text-[10px] text-zinc-500">미션 성공 확률</p></div></div><div className="mt-6 h-64"><ResponsiveContainer width="100%" height="100%"><BarChart data={data} margin={{top:10,right:8,left:-20,bottom:0}}><CartesianGrid stroke="#ffffff0d" vertical={false}/><XAxis dataKey="name" tick={{fill:'#a1a1aa',fontSize:12}} axisLine={false} tickLine={false}/><YAxis domain={[0,100]} tick={{fill:'#71717a',fontSize:11}} axisLine={false} tickLine={false} unit="%"/><Tooltip cursor={{fill:'#ffffff08'}} contentStyle={{background:'#090f1d',border:'1px solid #ffffff1a',borderRadius:12}} formatter={(value:number)=>[`${value}%`,'확률']}/><Bar dataKey="value" radius={[8,8,0,0]}>{data.map(item=><Cell key={item.name} fill={item.color}/>)}</Bar></BarChart></ResponsiveContainer></div><p className="mt-3 text-xs leading-5 text-zinc-500">Monte Carlo {result.monteCarloRuns}회에서 계산한 분포입니다. 실제 경기 결과에 대한 예측값이 아닙니다.</p></section>}
