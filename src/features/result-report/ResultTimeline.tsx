import type { SimulationEvent, SimulationEventType } from '../../entities/simulation/types'

const labels:Record<SimulationEventType,string>={build_up:'빌드업',progressive_pass:'전진 패스',wing_attack:'측면 공격',central_attack:'중앙 공격',box_entry:'박스 진입',shot:'슈팅',big_chance:'결정적 기회',goal:'골',save:'선방',turnover:'턴오버',counter_attack:'역습',foul:'파울',card:'카드',set_piece:'세트피스',defensive_block:'수비 블록',stamina_drop:'체력 저하'}
export function ResultTimeline({timeline,userTeamId}:{timeline:SimulationEvent[];userTeamId:string}){return <section className="rounded-2xl border border-white/10 bg-zinc-900/70 p-5 sm:p-7"><div><p className="text-xs font-black uppercase tracking-[.18em] text-green-400">Representative timeline</p><h2 className="mt-1 text-xl font-black">FM식 대표 경기 흐름</h2><p className="mt-2 text-xs text-zinc-500">확률 분포를 대표하는 한 번의 IF 시뮬레이션입니다.</p></div><div className="relative mt-7 space-y-1 before:absolute before:bottom-3 before:left-[27px] before:top-3 before:w-px before:bg-white/10">{timeline.map(event=><div key={event.id} className="relative grid grid-cols-[56px_1fr] gap-3 py-3"><div className={`relative z-10 grid size-14 place-items-center rounded-full border font-mono text-xs font-black ${event.type==='goal'?'border-green-400 bg-green-400 text-slate-950':event.isKeyEvent?'border-amber-400/40 bg-amber-400/10 text-amber-200':'border-white/10 bg-slate-950 text-zinc-400'}`}>{event.minute}′</div><div className={`rounded-xl border p-4 ${event.type==='goal'?'border-green-400/30 bg-green-400/10':'border-white/5 bg-slate-950/50'}`}><div className="flex flex-wrap items-center justify-between gap-2"><span className="text-[10px] font-black uppercase tracking-wider text-zinc-500">{labels[event.type]} · {event.teamId===userTeamId?'우리 팀':'상대 팀'}</span><b className="font-mono text-xs">{event.score.home} : {event.score.away}</b></div><p className="mt-2 text-sm leading-6 text-zinc-300">{event.text}</p></div></div>)}</div></section>}
