import { motion } from 'framer-motion'
import type { SimulationResult } from '../../entities/simulation/types'

export function ResultHeroCard({result}:{result:SimulationResult}){const success=Math.round(result.successProbability*100);return <motion.section initial={{opacity:0,y:24,scale:.98}} animate={{opacity:1,y:0,scale:1}} transition={{duration:.45,ease:[.2,.8,.2,1]}} className={`relative overflow-hidden rounded-3xl border p-7 sm:p-10 ${result.missionSuccess?'border-green-400/30 bg-green-400 text-slate-950':'border-amber-400/30 bg-amber-400 text-slate-950'}`}><div className="absolute -right-12 -top-16 size-56 rounded-full border-[40px] border-current opacity-[.06]"/><div className="relative grid gap-8 sm:grid-cols-[1fr_auto] sm:items-end"><div><p className="text-xs font-black uppercase tracking-[.2em] opacity-60">Data-based IF simulation</p><p className="mt-5 text-sm font-black">MISSION {result.missionSuccess?'COMPLETE':'FAILED'}</p><h1 className="mt-2 text-3xl font-black tracking-tight sm:text-5xl">{result.missionSuccess?'전술 선택이 가능성을 바꿨습니다':'이번 대표 흐름에서는 미션에 실패했습니다'}</h1><p className="mt-4 max-w-2xl text-sm leading-6 opacity-70">정답 예측이 아닌, 동일한 경기 시작점에서 전술 선택이 만든 하나의 대표 가능성입니다.</p></div><div className="sm:text-right"><p className="font-mono text-6xl font-black sm:text-7xl">{result.finalScore.home} : {result.finalScore.away}</p><p className="mt-2 text-xs font-black opacity-60">미션 성공 확률 {success}% · {result.monteCarloRuns}회 실행</p></div></div></motion.section>}
